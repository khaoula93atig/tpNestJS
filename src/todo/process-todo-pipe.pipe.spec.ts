import { ProcessTodoPipePipe } from './process-todo-pipe.pipe';

describe('ProcessTodoPipePipe', () => {
  it('should be defined', () => {
    expect(new ProcessTodoPipePipe()).toBeDefined();
  });
});
